
namespace PasswordManager.Bot.Commands.Enums; 

public enum SelectLanguageCommandCode {
	//This command is for purpose of showing invitation to select language
	//if user presses on "change lang" button
	// '!' user to prevent confusion with language codes
	SelectLanguage = '!'
}